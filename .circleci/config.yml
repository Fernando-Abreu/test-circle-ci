version: 2.1

jobs:
  test:
    type: no-op

  build:
    type: no-op

  deploy:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          exit 1

workflows:
  main-workflow:
    jobs:
      - test
      - build
      - deploy:
          serial-group: << pipeline.git.branch >>
          requires:
            - test
            - build