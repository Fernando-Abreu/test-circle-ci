version: 2.1

jobs:
  job1:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: "Running Job 1"
          command: |
            echo "<< pipeline.git.branch >>"

workflows:
  version: 2
  build_and_test:
    jobs:
      - job1
