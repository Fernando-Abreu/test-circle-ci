version: 2.1

jobs:
  test:
    type: no-op

  build:
    type: no-op

  deploy:
    type: no-op

workflows:
  main-workflow:
    jobs:
      - test
      - build
      - deploy:
          serial-group: << pipeline.git.branch >>
          requires:
            - test
            - build