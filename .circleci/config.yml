version: 2.1

jobs:
  job1:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: "Running Job 1"
          max_auto_reruns: 3
          auto_rerun_delay: 15s
          command: |
            echo "Job 1 is starting."
            exit 1


workflows:
  version: 2
  build_and_test:
    jobs:
      - job1